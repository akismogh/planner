name = "retirement-planner-ai"
main = "index.js"
compatibility_date = "2024-11-01"

# The Anthropic API key is NOT set here (never commit secrets to this file).
# Set it once with:
#   npx wrangler secret put ANTHROPIC_API_KEY
